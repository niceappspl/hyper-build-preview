import { SnackFile } from 'snack-content';
import { Logger } from './Logger';
import { SnackError } from './types';
export declare type FileUploaderRequest = {
    path: string;
    file: SnackFile;
};
export declare type FileUploaderCallback = (request: FileUploaderRequest, resultURL?: string, error?: SnackError) => any;
export default class FileUploader {
    private apiURL;
    private logger?;
    private status;
    private callback;
    constructor(options: {
        apiURL: string;
        callback: FileUploaderCallback;
        logger?: Logger;
    });
    add(path: string, file: SnackFile): Promise<any>;
    remove(path: string, file?: SnackFile): void;
    waitForCompletion(): Promise<void>;
    private upload;
}
