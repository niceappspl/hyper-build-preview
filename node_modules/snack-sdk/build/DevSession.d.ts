import { Logger } from './Logger';
import { SnackState, SnackSendBeaconRequest } from './types';
export default class DevSession {
    private apiURL;
    private logger?;
    private onSendBeaconCloseRequest;
    private focusedAt?;
    constructor(options: {
        apiURL: string;
        logger?: Logger;
        onSendBeaconCloseRequest: (request: SnackSendBeaconRequest) => any;
    });
    setState(state: SnackState, prevState: SnackState): void;
    setFocus(state: SnackState): void;
    private getRequest;
    private notify;
    private close;
}
