import { SDKVersion } from 'snack-content';
import { SnackError, SnackUser } from './types';
declare const fetch: typeof globalThis.fetch;
export { fetch };
export declare function createChannel(channel?: string): string;
export declare function createURL(endpoint: string, sdkVersion: SDKVersion, channel?: string, id?: string): string;
export declare function createError(config: {
    message: string;
    name?: string;
    fileName?: string;
    lineNumber?: number;
    columnNumber?: number;
    stack?: string;
}): SnackError;
export declare function createUserHeader(user?: SnackUser): {
    [key: string]: string;
};
