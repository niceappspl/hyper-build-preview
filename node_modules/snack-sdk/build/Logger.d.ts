declare type LogFn = (...messages: unknown[]) => void;
export interface Logger {
    info: LogFn;
    warn: LogFn;
    error: LogFn;
    comm: LogFn;
    comm_recv: LogFn;
    module: LogFn;
}
export declare function createLogger(isEnabled: boolean): Logger;
export {};
